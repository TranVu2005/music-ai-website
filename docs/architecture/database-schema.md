# Sơ đồ Cơ sở Dữ liệu (Database Schema)

Tài liệu liệt kê các bảng và danh sách các cột dự kiến trong cơ sở dữ liệu hệ thống:

## 1. Bảng `users`
- TODO: `id` (UUID / Primary Key)
- TODO: `email` (VARCHAR, Unique)
- TODO: `password_hash` (VARCHAR)
- TODO: `full_name` (VARCHAR)
- TODO: `phone_number` (VARCHAR)
- TODO: `role` (ENUM: 'admin', 'customer')
- TODO: `created_at` (TIMESTAMP)
- TODO: `updated_at` (TIMESTAMP)

## 2. Bảng `tracks`
- TODO: `id` (UUID / Primary Key)
- TODO: `title` (VARCHAR)
- TODO: `slug` (VARCHAR, Unique)
- TODO: `description` (TEXT)
- TODO: `genre` (VARCHAR)
- TODO: `mood` (VARCHAR)
- TODO: `bpm` (INTEGER)
- TODO: `duration_seconds` (INTEGER)
- TODO: `preview_file_url` (VARCHAR - file MP3 chất lượng thấp chèn watermark âm thanh)
- TODO: `original_file_key` (VARCHAR - đường dẫn lưu trữ file master WAV/FLAC trong private bucket)
- TODO: `cover_image_url` (VARCHAR)
- TODO: `status` (ENUM: 'draft', 'published', 'reserved', 'sold_exclusive', 'archived')
- TODO: `reserved_until` (TIMESTAMP, Nullable - thời hạn tạm khóa khi thanh toán độc quyền)
- TODO: `created_at` (TIMESTAMP)
- TODO: `updated_at` (TIMESTAMP)

## 3. Bảng `licenses`
- TODO: `id` (UUID / Primary Key)
- TODO: `track_id` (UUID, Foreign Key -> tracks.id)
- TODO: `license_type` (ENUM: 'standard', 'exclusive')
- TODO: `price` (DECIMAL / BIGINT - đơn vị VNĐ)
- TODO: `terms_summary` (TEXT)
- TODO: `is_active` (BOOLEAN)
- TODO: `created_at` (TIMESTAMP)
- TODO: `updated_at` (TIMESTAMP)

## 4. Bảng `orders`
- TODO: `id` (UUID / Primary Key)
- TODO: `order_code` (VARCHAR, Unique)
- TODO: `user_id` (UUID, Foreign Key -> users.id, Nullable cho khách mua không đăng nhập)
- TODO: `customer_email` (VARCHAR)
- TODO: `customer_name` (VARCHAR)
- TODO: `customer_phone` (VARCHAR)
- TODO: `track_id` (UUID, Foreign Key -> tracks.id)
- TODO: `license_id` (UUID, Foreign Key -> licenses.id)
- TODO: `amount` (DECIMAL / BIGINT)
- TODO: `payment_status` (ENUM: 'pending', 'paid', 'failed', 'cancelled', 'refunded')
- TODO: `payment_method` (VARCHAR: 'bank_transfer_qr', 'payos', etc.)
- TODO: `transaction_id` (VARCHAR, Unique, Nullable)
- TODO: `download_count` (INTEGER, Default: 0)
- TODO: `download_expires_at` (TIMESTAMP, Nullable)
- TODO: `license_pdf_url` (VARCHAR, Nullable)
- TODO: `created_at` (TIMESTAMP)
- TODO: `updated_at` (TIMESTAMP)

## 5. Bảng `custom_requests`
- TODO: `id` (UUID / Primary Key)
- TODO: `user_id` (UUID, Foreign Key -> users.id, Nullable)
- TODO: `customer_name` (VARCHAR)
- TODO: `customer_email` (VARCHAR)
- TODO: `customer_phone` (VARCHAR)
- TODO: `brief_description` (TEXT)
- TODO: `reference_links` (TEXT)
- TODO: `genre_preference` (VARCHAR)
- TODO: `target_duration` (VARCHAR)
- TODO: `budget_estimate` (DECIMAL / BIGINT, Nullable)
- TODO: `quoted_price` (DECIMAL / BIGINT, Nullable)
- TODO: `deposit_amount` (DECIMAL / BIGINT, Nullable)
- TODO: `remaining_amount` (DECIMAL / BIGINT, Nullable)
- TODO: `status` (ENUM: 'submitted', 'quoted', 'deposit_pending', 'in_progress', 'demo_sent', 'revising', 'approved', 'completed', 'cancelled')
- TODO: `demo_file_url` (VARCHAR, Nullable)
- TODO: `final_file_key` (VARCHAR, Nullable)
- TODO: `revision_limit` (INTEGER, Default: 2)
- TODO: `revision_used` (INTEGER, Default: 0)
- TODO: `notes` (TEXT, Nullable)
- TODO: `created_at` (TIMESTAMP)
- TODO: `updated_at` (TIMESTAMP)
